spring:
  profiles:
    active: dev  # default using dev env
  datasource:
    url: ${RAILWAY_MYSQL_URL}
    username: ${RAILWAY_MYSQL_USERNAME}
    password: ${RAILWAY_MYSQL_PASSWORD}
    driver-class-name: ${RAILWAY_MYSQL_DRIVER_CLASSNAME}

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    # TODO: need to print to the log file
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

aliyun:
  oss:
    endpoint: "${ALIYUN_OSS_ENDPOINT}"
    access-key-id: "${ALIYUN_OSS_ACCESS_KEY_ID}"
    access-key-secret: "${ALIYUN_OSS_ACCESS_KEY_SECRET}"
    bucket-name: "${ALIYUN_OSS_BUCKET_NAME}"

okta:
  client-id: "${OKTA_CLIENT_ID}"
  client-secret: "${OKTA_CLIENT_SECRET}"
  redirect-uri: "${OKTA_REDIRECT_URI}"
  domain: "${OKTA_DOMAIN}"
  authServer: "${OKTA_AUTH_SERVER}"

serverchan:
  token: "${SERVER_CHAN_KEY}"

